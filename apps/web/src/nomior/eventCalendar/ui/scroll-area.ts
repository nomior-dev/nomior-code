/**
 * The vendored calendar's scroll area. Same name, same component; re-exported
 * here only so every `./ui/*` import in the vendored files resolves in one
 * place.
 *
 * @module nomior/eventCalendar/ui/scroll-area
 */
export { ScrollArea, ScrollBar } from "../../../components/ui/scroll-area";
