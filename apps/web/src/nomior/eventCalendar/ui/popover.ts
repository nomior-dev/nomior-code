/**
 * The vendored calendar's popover, under the names it imports. A rename, for
 * the same reason as `./tooltip`.
 *
 * @module nomior/eventCalendar/ui/popover
 */
export {
  Popover,
  PopoverTrigger,
  PopoverPopup as PopoverContent,
} from "../../../components/ui/popover";
