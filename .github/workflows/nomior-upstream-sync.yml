# Daily upstream sync: merge the newest unmerged upstream STABLE tag into a
# sync/upstream-<tag> branch and open a labeled PR. Auto-merge is deliberately
# NOT enabled yet — it turns on only after several observed clean runs;
# until then a human merges the PR.
#
# Uses NOMIOR_SYNC_TOKEN (a PAT with contents+pull-requests write) when
# configured so the pushed branch and PR trigger CI; the default GITHUB_TOKEN
# works too but GitHub suppresses workflow runs for pushes it makes.
name: Nomior Upstream Sync

on:
  schedule:
    - cron: "17 5 * * *"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  issues: write

concurrency:
  group: nomior-upstream-sync
  cancel-in-progress: false

env:
  UPSTREAM_REPO: https://github.com/pingdotgg/t3code.git
  # Every step below adds an `upstream` remote, and `gh` with no default repo
  # picks one from the remotes — it chose upstream, so `gh pr create` and every
  # issue call aimed at pingdotgg/t3code and 403'd. Pin the target.
  GH_REPO: ${{ github.repository }}

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
          token: ${{ secrets.NOMIOR_SYNC_TOKEN || github.token }}

      - name: Configure git identity
        run: |
          git config user.name "nomior-sync-bot"
          git config user.email "sync-bot@users.noreply.github.com"

      - name: Fetch upstream
        run: |
          git remote add upstream "$UPSTREAM_REPO"
          git fetch upstream --prune 'refs/tags/*:refs/tags/*' '+refs/heads/main:refs/remotes/upstream/main'

      - name: Resolve newest unmerged upstream stable tag
        id: tag
        run: |
          set -euo pipefail
          # Stable = bare vX.Y.Z only. A positive match also keeps out any
          # other prerelease suffix (-beta.N, -rc.N), not just -nightly.
          tag=$(git for-each-ref 'refs/tags/v*' --sort=-v:refname --format='%(refname:strip=2)' \
            | grep -E -m 1 '^v[0-9]+\.[0-9]+\.[0-9]+$' || true)
          # `caught_up` and not merely an empty tag: finding no stable tag at
          # all also leaves `tag` empty, and reading that as "level with
          # upstream" would close the drift issue on the one result that most
          # deserves it.
          if [ -z "$tag" ]; then
            echo "No stable upstream tag found."
            echo "tag=" >> "$GITHUB_OUTPUT"
            echo "caught_up=" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          # How long the fork has been behind, for the drift report below.
          released=$(git for-each-ref "refs/tags/$tag" --format='%(creatordate:unix)')
          echo "age_days=$(( ( $(date +%s) - released ) / 86400 ))" >> "$GITHUB_OUTPUT"
          if git merge-base --is-ancestor "$tag" HEAD; then
            echo "Newest stable tag $tag is already merged; nothing to sync."
            echo "tag=" >> "$GITHUB_OUTPUT"
            echo "caught_up=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "caught_up=" >> "$GITHUB_OUTPUT"
          echo "Syncing to $tag"
          echo "tag=$tag" >> "$GITHUB_OUTPUT"

      # Each run recreates the sync branch from main and force-pushes it. If a
      # human pushed fix commits onto the open sync PR, that would silently
      # discard them — so when the remote branch carries any commit not
      # authored by nomior-sync-bot, leave branch and PR alone until the PR is
      # merged or closed.
      - name: Guard existing sync branch
        if: steps.tag.outputs.tag != ''
        id: guard
        run: |
          set -euo pipefail
          TAG="${{ steps.tag.outputs.tag }}"
          skip=false
          if git rev-parse -q --verify "refs/remotes/origin/sync/upstream-$TAG" >/dev/null; then
            foreign=$(git log --format='%an' "origin/sync/upstream-$TAG" --not main "$TAG" \
              | grep -Fxv 'nomior-sync-bot' | sort -u || true)
            if [ -n "$foreign" ]; then
              echo "sync/upstream-$TAG has commits not authored by nomior-sync-bot:"
              printf '%s\n' "$foreign"
              echo "Leaving the branch and its PR untouched."
              skip=true
            fi
          fi
          echo "skip=$skip" >> "$GITHUB_OUTPUT"

      - name: Setup Node
        if: steps.tag.outputs.tag != '' && steps.guard.outputs.skip != 'true'
        uses: actions/setup-node@v4
        with:
          node-version-file: package.json

      - name: Setup pnpm via corepack
        if: steps.tag.outputs.tag != '' && steps.guard.outputs.skip != 'true'
        run: corepack enable

      # Install before merging so the manifest checker's dependencies come from
      # a known-good lockfile state (main). The checker only needs the scripts
      # workspace.
      - name: Install manifest-check dependencies
        if: steps.tag.outputs.tag != '' && steps.guard.outputs.skip != 'true'
        run: pnpm install --frozen-lockfile --filter @t3tools/scripts...

      - name: Merge upstream tag
        if: steps.tag.outputs.tag != '' && steps.guard.outputs.skip != 'true'
        run: |
          set -euo pipefail
          TAG="${{ steps.tag.outputs.tag }}"
          git switch -C "sync/upstream-$TAG" main
          if ! git merge --no-ff --no-edit "$TAG"; then
            conflicted=$(git diff --name-only --diff-filter=U)
            if [ "$conflicted" = "pnpm-lock.yaml" ]; then
              # Lockfile-only conflict: mechanical resolution per the policy in
              # docs/nomior/FORK-MANIFEST.md.
              sh scripts/nomior/resolve-lockfile.sh
              git commit --no-edit
            else
              echo "Sync merge of $TAG conflicts beyond pnpm-lock.yaml:" >&2
              printf '%s\n' "$conflicted" >&2
              echo "Resolve manually: git fetch upstream && git merge $TAG on a sync branch." >&2
              exit 1
            fi
          fi

      - name: Report manifest impact
        if: steps.tag.outputs.tag != '' && steps.guard.outputs.skip != 'true'
        id: manifest
        run: |
          set -euo pipefail
          node scripts/nomior/check-fork-manifest.ts --print-manifest-paths > /tmp/manifest-paths.txt
          git diff --name-only main HEAD | sort -u > /tmp/sync-changed.txt
          touched=$(grep -Fxf /tmp/manifest-paths.txt /tmp/sync-changed.txt || true)
          {
            echo "touched<<EOF"
            echo "$touched"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"
          # The full check is the tripwire for upstream deleting or rewriting a
          # file we modify; its result goes into the PR body, review decides.
          if node scripts/nomior/check-fork-manifest.ts; then
            echo "check=pass" >> "$GITHUB_OUTPUT"
          else
            echo "check=fail" >> "$GITHUB_OUTPUT"
          fi

      - name: Push sync branch
        if: steps.tag.outputs.tag != '' && steps.guard.outputs.skip != 'true'
        run: git push --force-with-lease origin "HEAD:refs/heads/sync/upstream-${{ steps.tag.outputs.tag }}"

      - name: Open or update pull request
        if: steps.tag.outputs.tag != '' && steps.guard.outputs.skip != 'true'
        env:
          GH_TOKEN: ${{ secrets.NOMIOR_SYNC_TOKEN || github.token }}
          TAG: ${{ steps.tag.outputs.tag }}
          TOUCHED: ${{ steps.manifest.outputs.touched }}
          CHECK: ${{ steps.manifest.outputs.check }}
        run: |
          set -euo pipefail
          gh label create upstream-sync --description "Automated upstream sync PR" --color 1d76db || true

          if [ -n "$TOUCHED" ]; then
            touched_list=$(printf '%s\n' "$TOUCHED" | sed 's/^/- `/;s/$/`/')
            touched_section="Manifest'd upstream files TOUCHED by this sync — review required:
          $touched_list"
          else
            touched_section="No manifest'd upstream file was touched by this sync."
          fi

          body=$(cat <<EOF
          Automated sync of upstream stable tag \`$TAG\`.

          $touched_section

          Fork-manifest check after merge: **$CHECK** (\`pnpm nomior:check-manifest\`)

          Auto-merge is intentionally not enabled yet: merge manually once checks are green.
          EOF
          )

          existing=$(gh pr list --head "sync/upstream-$TAG" --state open --json number --jq '.[0].number // empty')
          if [ -n "$existing" ]; then
            gh pr edit "$existing" --body "$body"
          else
            gh pr create \
              --base main \
              --head "sync/upstream-$TAG" \
              --title "chore(sync): merge upstream $TAG" \
              --label upstream-sync \
              --body "$body"
          fi

      # A sync that dies (a conflict beyond the lockfile, an expired token, a
      # runner that never starts) used to be visible only to whoever thought to
      # open the Actions tab. Same reporting as the canary: one open issue,
      # commented on each further failure.
      - name: Report failure as issue
        if: failure()
        env:
          GH_TOKEN: ${{ secrets.NOMIOR_SYNC_TOKEN || github.token }}
          TAG: ${{ steps.tag.outputs.tag }}
        run: |
          set -euo pipefail
          gh label create upstream-sync-failed --description "Upstream sync run failures" --color b60205 || true
          run_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          existing=$(gh issue list --label upstream-sync-failed --state open --json number --jq '.[0].number // empty')
          if [ -n "$existing" ]; then
            gh issue comment "$existing" --body "Sync still failing (target \`${TAG:-none}\`) — [run]($run_url)"
          else
            gh issue create \
              --title "Upstream sync failing" \
              --label upstream-sync-failed \
              --body "The daily upstream sync failed — [run]($run_url). Until it runs clean the fork stops receiving upstream releases."
          fi

      # Opening the PR is not the same as being up to date: the merge is a
      # human's call, so an unmerged sync PR drifts silently. Past the
      # threshold that becomes an issue, and landing the tag closes it.
      - name: Report drift
        # `steps.tag.outcome` guards the close branch: a tag step that itself
        # died also reports an empty tag, and that is not "caught up".
        if: always() && steps.tag.outcome == 'success' && (steps.tag.outputs.caught_up == 'true' || steps.tag.outputs.age_days >= 7)
        env:
          GH_TOKEN: ${{ secrets.NOMIOR_SYNC_TOKEN || github.token }}
          TAG: ${{ steps.tag.outputs.tag }}
          CAUGHT_UP: ${{ steps.tag.outputs.caught_up }}
          AGE: ${{ steps.tag.outputs.age_days }}
        run: |
          set -euo pipefail
          gh label create upstream-drift --description "Fork is behind upstream stable" --color fbca04 || true
          existing=$(gh issue list --label upstream-drift --state open --json number --jq '.[0].number // empty')
          if [ "$CAUGHT_UP" = "true" ]; then
            # Caught up. Reverse state: the issue that announced the drift is
            # the issue that has to close when it ends.
            if [ -n "$existing" ]; then
              gh issue close "$existing" --comment "Fork is level with upstream stable again."
            fi
            exit 0
          fi
          run_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          note="Upstream stable \`$TAG\` has been out for $AGE days and is still not on \`main\`. Its sync PR is open and unmerged, or the sync is blocked — [run]($run_url)."
          if [ -n "$existing" ]; then
            gh issue comment "$existing" --body "$note"
          else
            gh issue create --title "Fork is behind upstream stable $TAG" --label upstream-drift --body "$note"
          fi
